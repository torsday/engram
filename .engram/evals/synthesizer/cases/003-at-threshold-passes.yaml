# Case 003 — at-threshold proposal.
#
# A cluster that just barely coheres around one concept — enough to
# propose, but the naming is non-obvious. Confidence should sit near the
# 0.80 discard floor: high enough to convene a council, honest about the
# borderline coherence.
id: 003-at-threshold-passes
description: >
  A borderline-but-coherent cluster. Expect a proposal with confidence
  at or just above the council-convening floor.
input:
  vault_state: snapshot/cases/003/at-threshold-cluster.tar
expected:
  min_confidence: 0.80
scoring:
  precision_weight: 1.0
  recall_weight: 1.0
  calibration_weight: 1.0
  cost_weight: 0.2
