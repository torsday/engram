# Case 004 — merge reassigns inbound links to the canonical.
#
# The duplicate pair has inbound links from several other notes. Merger's
# proposed_merge must include link_reassignments pointing each inbound
# anchor at the canonical (and, where possible, the right section) so no
# link is left dangling at a note that is about to be subsumed.
id: 004-link-reassignment
description: >
  A duplicate pair with inbound links. Expect link reassignments routing
  each inbound anchor to the canonical note.
input:
  vault_state: snapshot/cases/004/duplicate-with-inbound-links.tar
expected:
  min_confidence: 0.85
scoring:
  precision_weight: 1.0
  recall_weight: 1.0
  calibration_weight: 1.0
  cost_weight: 0.2
