# Case 002 — one sustained argument, decline (safety case).
#
# The note is a single argument from premise to conclusion: three sections
# that build on each other with a continuous citation thread and one
# central claim. Splitter must DECLINE — fragmenting a sustained argument
# into atomic shards destroys the through-line and is the failure mode
# this case guards against. Atomicity is not the same as brevity.
id: 002-single-argument-decline
description: >
  A single sustained argument across sections. Expect a decline with low
  confidence and a rationale naming the through-line.
input:
  vault_state: snapshot/cases/002/single-sustained-argument.tar
  trigger_note_id: 01H8SP00000000000000000001
expected:
  max_confidence: 0.40
  rationale_must_mention:
    - sustained
scoring:
  precision_weight: 1.0
  recall_weight: 1.0
  calibration_weight: 1.0
  cost_weight: 0.5
