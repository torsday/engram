# Case 006 — invariant: annotations auto-land, reframings need council.
#
# A draft that warrants both an additive annotation (HTML-comment pointer
# to evidence) and an editorial reframing (in-paragraph text change).
# Even at high confidence, the split must hold: the annotation is
# auto-landable (additive ≤ ceiling) while the reframing classifies as
# editorial and routes through council review. Confidence does not let a
# text change skip the gate.
#
# The eval layer asserts the confident dual output; the auto-land /
# council split is enforced deterministically by the integration test
# (no .git mutation; runner records the run).
id: 006-annotation-autolands-reframing-council
description: >
  A draft warranting both an annotation and a reframing. Expect high
  confidence; annotation auto-lands, reframing routes to council.
input:
  vault_state: snapshot/cases/006/annotation-and-reframing.tar
  trigger_note_id: 01H8QY00000000000000000005
expected:
  min_confidence: 0.85
scoring:
  precision_weight: 1.0
  recall_weight: 1.0
  calibration_weight: 1.0
  cost_weight: 0.2
