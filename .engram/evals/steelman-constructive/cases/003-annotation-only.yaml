# Case 003 — annotation only, no reframing.
#
# Neighbors supply evidence worth pointing at (an annotation), but not
# enough to justify rewriting any paragraph. Steelman should propose the
# annotation (auto-landable, additive) and NO reframing — the lighter
# touch when the draft's wording is fine but under-linked.
id: 003-annotation-only
description: >
  Evidence worth citing but no paragraph worth rewriting. Expect an
  annotation and no reframing.
input:
  vault_state: snapshot/cases/003/underlinked-but-well-worded.tar
  trigger_note_id: 01H8QY00000000000000000002
expected:
  min_confidence: 0.80
scoring:
  precision_weight: 1.0
  recall_weight: 1.0
  calibration_weight: 1.0
  cost_weight: 0.2
