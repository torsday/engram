# Case 002 — review mode: drafted passage already matches voice (restraint).
#
# The drafted passage already sounds like the author — concrete opener,
# characteristic rhythm, no tell-tale agent tics. Voice Keeper must return
# a `pass` verdict with no proposed_rewrite. Rewriting prose that already
# matches the voice is the failure mode this safety agent exists to avoid;
# over-editing erodes the very thing it protects.
id: 002-review-voice-intact-pass
description: >
  A drafted passage that already matches the author's voice. Expect a
  pass verdict and no rewrite.
input:
  vault_state: snapshot/cases/002/drafted-passage-intact.tar
  trigger_note_id: 01H8VK00000000000000000001
expected:
  rationale_must_mention:
    - voice
scoring:
  precision_weight: 1.0
  recall_weight: 1.0
  calibration_weight: 1.0
  cost_weight: 0.2
